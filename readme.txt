Install instructions
==========================
Make sure the execution policy is set to, at least, RemoteSigned.
To check execution policy, run the command:
  Get-ExecutionPolicy
To change execution policy, run the command (as administrator):
  Set-Execution-Policy RemoteSigned

Run the install.ps1 script with thw command:
  ./install.ps1

Done! :-)

